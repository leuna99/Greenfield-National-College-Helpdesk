# Greenfield Nation College Student Helpdesk Chatbot System

BCA 8th semester full-stack web project using Python Flask, SQLite, HTML, CSS and JavaScript.

## Features
- Student login with automatic first-time account creation
- Secure password hashing
- FAQ-based chatbot using fuzzy matching
- Per-student chat history
- Helpdesk ticket creation
- Admin login
- Admin ticket management and replies
- FAQ management
- Basic analytics dashboard
- Responsive colorful UI

## Run in VS Code

1. Open this folder in VS Code.
2. Open Terminal.
3. Create a virtual environment:

   `python -m venv venv`

4. Activate it on Windows PowerShell:

   `venv\Scripts\Activate.ps1`

   If PowerShell blocks activation, use Command Prompt:

   `venv\Scripts\activate.bat`

5. Install packages:

   `pip install -r requirements.txt`

6. Start the application:

   `python app.py`

7. Open:

   `http://127.0.0.1:5000`

## Demo administrator account
Username: `admin`
Password: `admin123`

Change the secret key and administrator password before using this beyond a classroom demo.
